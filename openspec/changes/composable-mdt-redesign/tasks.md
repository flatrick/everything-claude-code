## 1. Compose Contract

- [ ] 1.1 Define the package/dependency metadata shape needed to declare composed skill inputs.
- [ ] 1.2 Define the staging-versus-install parity contract for composed skills.
- [ ] 1.3 Confirm the first real non-ai-learning consumer before implementation begins.

## 2. Resolver And Assembly Design

- [ ] 2.1 Design how the shared resolver assembles composed skill output without bypass paths.
- [ ] 2.2 Define deterministic parity checks between staged output and installed output.
- [ ] 2.3 Define migration boundaries so current non-composed installs continue to work during rollout.

## 3. Implementation Readiness

- [ ] 3.1 Finish the current ai-learning self-contained change before starting compose implementation.
- [ ] 3.2 Add verification coverage for composed output structure and parity once implementation starts.
- [ ] 3.3 Update current-state docs only after compose behavior is implemented and locally verified.
