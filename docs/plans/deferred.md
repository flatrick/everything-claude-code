# Deferred Notes

This page holds speculative or intentionally deferred ideas that are not active OpenSpec changes and are not current implementation plans.

Use this file sparingly:
- keep only items that are intentionally deferred rather than merely unscoped
- move an item into `openspec/changes/` once it becomes a real scoped change
- move an item into `docs/plans/details/` once it becomes an active repo plan

## Deferred Items

### Migrate Node runtime scripts to `.mjs` after `v1.0.0` stabilization

**Status:** Deferred.

Open questions:
- target module policy
- rollout and migration strategy
- compatibility boundary for scripts, tests, and install runtime

### Revisit OpenCode after `v1.0.0` plus `.mjs` migration

**Status:** Deferred.

This remains speculative until the runtime-format decision is made.
