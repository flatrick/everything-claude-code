# Merge continuous-learning-* into ai-learning

- Archive reason: completed implementation shipped; archived on `2026-03-14` after plan index cleanup.

- Status: `finished`
- Created: `2026-03-13`
- Last updated: `2026-03-14`
- Owner: `mdt`
- Execution mode: `agent`
- Related docs: `skills/continuous-learning-automatic/SKILL.md`, `skills/continuous-learning-manual/SKILL.md`, `codex-template/skills/continuous-learning-manual/SKILL.md`, `BACKLOG.md`

## Goal

- [x] Replace `continuous-learning-automatic` and `continuous-learning-manual` with a single `ai-learning` skill that adapts its observation and analysis path to the environment: hooks-passive for Claude/Cursor, manual/observer-optional for Codex and hook-free tools.

## Non-goals

- [ ] Do not merge `/learn` and `/learn-eval` commands — they serve distinct use-cases (fast capture vs quality-gated extraction with rubric).
- [ ] Do not enable the observer by default in the Codex template — the observer is always optional in hook-free environments.
- [ ] Do not change the instinct storage schema, confidence model, or project-detection logic.
- [ ] Do not remove the `mdt learning` CLI surface (capture, analyze, status, retrospective, observer).
- [ ] Do not change the instinct management commands (instinct-status, evolve, instinct-export, instinct-import, promote, projects).

## Open Questions / Assumptions

- [x] `/learn` and `/learn-eval` stay separate: confirmed by reviewing both commands — they are not equivalent.
- [x] `codex-template` keeps `agents/openai.yaml` — it is Codex-specific and has no counterpart in the shared skills.
- [x] Observer enabled by default for Claude/Cursor (`config.json` `observer.enabled: true`), disabled by default for Codex (`codex-template` `config.json` `observer.enabled: false`).
- [x] No `hooks/` directory in the codex-template version of `ai-learning` — Codex does not use MDT hooks.
- [x] `detect-project.js` appears in both skills; the copies are functionally identical — keep one copy in `ai-learning/scripts/`.

## Decisions

- [x] New skill name: `ai-learning` (replaces both `continuous-learning-automatic` and `continuous-learning-manual`).
- [x] Single `skill.meta.json`: hooks mode `optional`, tools `["claude", "cursor"]` — installs everywhere, hooks used when available.
- [x] Running the `/learn` command or `mdt learning` in a hook-enabled environment triggers the same analysis pipeline as the background observer — there is no separate "automatic" code path; hooks are only the observation capture source.
- [x] The external observer fills the hook role in hook-free environments when opted in — no special code path needed, same observer binary.
- [x] codex-template gets its own `skills/ai-learning/` mirroring the main skill minus `hooks/` plus `agents/openai.yaml`.

## Workstreams

### Workstream: create ai-learning skill

- Status: `completed`
- [x] Create `skills/ai-learning/` directory.
- [x] Copy `skills/continuous-learning-manual/scripts/` → `skills/ai-learning/scripts/` (codex-learn.js, instinct-cli.js, detect-project.js, retrospect-week.js).
- [x] Copy `skills/continuous-learning-manual/agents/` → `skills/ai-learning/agents/` (observer.md, start-observer.js).
- [x] Copy `skills/continuous-learning-automatic/hooks/observe.js` → `skills/ai-learning/hooks/observe.js`.
- [x] Write `skills/ai-learning/config.json` with `observer.enabled: true`.
- [x] Write `skills/ai-learning/skill.meta.json` — merge requirements from both skills; hooks mode `optional`.
- [x] Write `skills/ai-learning/SKILL.md` — unified, tool-mode-aware documentation covering both the hooks path and the manual/observer path.

### Workstream: create codex-template ai-learning skill

- Status: `completed`
- [x] Create `codex-template/skills/ai-learning/` directory.
- [x] Copy scripts and agents from `codex-template/skills/continuous-learning-manual/` (includes `agents/openai.yaml`).
- [x] Write `codex-template/skills/ai-learning/config.json` with `observer.enabled: false`.
- [x] Copy `skill.meta.json` from `codex-template/skills/continuous-learning-manual/` (no hooks entry).
- [x] Write `codex-template/skills/ai-learning/SKILL.md` — Codex-framed: manual-first, observer optional.

### Workstream: remove old skill directories

- Status: `completed`
- [x] Delete `skills/continuous-learning-automatic/`.
- [x] Delete `skills/continuous-learning-manual/`.
- [x] Delete `codex-template/skills/continuous-learning-manual/`.

### Workstream: update references

- Status: `completed`
- [x] Update `.claude-plugin/plugin.json` — replace both old skill names with `ai-learning`.
- [x] Update all package manifests (`packages/`) that reference `continuous-learning-automatic` or `continuous-learning-manual`.
- [x] Update `scripts/install-mdt.js` — replace skill name constants and baseline/package skill lists.
- [x] Update `scripts/mdt.js` — update any hardcoded skill path references that point to old skill directories.
- [x] Update `AGENTS.md` if it references either old skill name.
- [x] Grep repo-wide for remaining `continuous-learning-automatic` and `continuous-learning-manual` references; fix or note each one.

### Workstream: update tests

- Status: `completed`
- [x] Update `tests/scripts/install-mdt.test.js` — replace old skill names with `ai-learning`.
- [x] Update `tests/scripts/install-mdt-unit.test.js` — same.
- [x] Update `tests/scripts/continuous-learning-wrapper.test.js` — update skill root paths to `ai-learning`.
- [x] Update `tests/scripts/mdt-cli.test.js` — update any skill path references.
- [x] Update `tests/hooks/hooks.test.js` if it references old skill names.
- [x] Run `npm test` — all 1297 tests must pass.

### Workstream: update docs

- Status: `completed`
- [x] Update `BACKLOG.md` — mark old skill split as resolved, note new unified skill.
- [x] Update `docs/plans/active.md` — add this plan as `in-progress`.
- [x] Check `docs/` for any references to old skill names and update.

## Verification

- [x] `npm test` passes (1297 tests, 0 failures).
- [x] `npm run lint` passes.
- [x] `ls skills/ai-learning/` contains: SKILL.md, config.json, skill.meta.json, hooks/, scripts/, agents/.
- [x] `ls codex-template/skills/ai-learning/` contains: SKILL.md, config.json, skill.meta.json, scripts/, agents/ (no hooks/).
- [x] `codex-template/skills/ai-learning/agents/openai.yaml` exists.
- [x] `skills/continuous-learning-automatic/` does not exist.
- [x] `skills/continuous-learning-manual/` does not exist.
- [x] `codex-template/skills/continuous-learning-manual/` does not exist.
- [x] `grep -r "continuous-learning-automatic\|continuous-learning-manual" skills/ codex-template/ scripts/ tests/` returns no matches.
- [x] Observer is enabled in `skills/ai-learning/config.json` (`"enabled": true`).
- [x] Observer is disabled in `codex-template/skills/ai-learning/config.json` (`"enabled": false`).
- [x] `hooks/observe.js` exists in `skills/ai-learning/` but not in `codex-template/skills/ai-learning/`.

## Follow-ups

- [ ] Rename commands that reference "continuous-learning" in their body text (learn.md, learn-eval.md) to reference `ai-learning` if needed.
- [ ] Consider whether `commands/learn.md` and `commands/learn-eval.md` should be moved inside `ai-learning/` as skill-owned commands or stay as top-level shared commands.
- [ ] Evaluate whether `SKILL.md` for ai-learning should be split into a short user-facing card + a detailed reference, to reduce cognitive load for first-time readers.
