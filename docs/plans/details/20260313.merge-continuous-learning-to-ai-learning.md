# Merge continuous-learning-* into ai-learning

- Status: `finished`
- Created: `2026-03-13`
- Last updated: `2026-03-13`
- Owner: `mdt`
- Execution mode: `agent`
- Related docs: `skills/continuous-learning-automatic/SKILL.md`, `skills/continuous-learning-manual/SKILL.md`, `codex-template/skills/continuous-learning-manual/SKILL.md`, `BACKLOG.md`

## Goal

- [ ] Replace `continuous-learning-automatic` and `continuous-learning-manual` with a single `ai-learning` skill that adapts its observation and analysis path to the environment: hooks-passive for Claude/Cursor, manual/observer-optional for Codex and hook-free tools.

## Non-goals

- [ ] Do not merge `/learn` and `/learn-eval` commands — they serve distinct use-cases (fast capture vs quality-gated extraction with rubric).
- [ ] Do not enable the observer by default in the Codex template — the observer is always optional in hook-free environments.
- [ ] Do not change the instinct storage schema, confidence model, or project-detection logic.
- [ ] Do not remove the `mdt learning` CLI surface (capture, analyze, status, retrospective, observer).
- [ ] Do not change the instinct management commands (instinct-status, evolve, instinct-export, instinct-import, promote, projects).

## Open Questions / Assumptions

- [x] `/learn` and `/learn-eval` stay separate: confirmed by reviewing both commands — they are not equivalent.
- [x] `codex-template` keeps `agents/openai.yaml` — it is Codex-specific and has no counterpart in the shared skills.
- [x] Observer enabled by default for Claude/Cursor (`config.json` `observer.enabled: true`), disabled by default for Codex (`codex-template` `config.json` `observer.enabled: false`).
- [x] No `hooks/` directory in the codex-template version of `ai-learning` — Codex does not use MDT hooks.
- [x] `detect-project.js` appears in both skills; the copies are functionally identical — keep one copy in `ai-learning/scripts/`.

## Decisions

- [x] New skill name: `ai-learning` (replaces both `continuous-learning-automatic` and `continuous-learning-manual`).
- [x] Single `skill.meta.json`: hooks mode `optional`, tools `["claude", "cursor"]` — installs everywhere, hooks used when available.
- [x] Running the `/learn` command or `mdt learning` in a hook-enabled environment triggers the same analysis pipeline as the background observer — there is no separate "automatic" code path; hooks are only the observation capture source.
- [x] The external observer fills the hook role in hook-free environments when opted in — no special code path needed, same observer binary.
- [x] codex-template gets its own `skills/ai-learning/` mirroring the main skill minus `hooks/` plus `agents/openai.yaml`.

## Workstreams

### Workstream: create ai-learning skill

- Status: `not-started`
- [ ] Create `skills/ai-learning/` directory.
- [ ] Copy `skills/continuous-learning-manual/scripts/` → `skills/ai-learning/scripts/` (codex-learn.js, instinct-cli.js, detect-project.js, retrospect-week.js).
- [ ] Copy `skills/continuous-learning-manual/agents/` → `skills/ai-learning/agents/` (observer.md, start-observer.js).
- [ ] Copy `skills/continuous-learning-automatic/hooks/observe.js` → `skills/ai-learning/hooks/observe.js`.
- [ ] Write `skills/ai-learning/config.json` with `observer.enabled: true`.
- [ ] Write `skills/ai-learning/skill.meta.json` — merge requirements from both skills; hooks mode `optional`.
- [ ] Write `skills/ai-learning/SKILL.md` — unified, tool-mode-aware documentation covering both the hooks path and the manual/observer path.

### Workstream: create codex-template ai-learning skill

- Status: `not-started`
- [ ] Create `codex-template/skills/ai-learning/` directory.
- [ ] Copy scripts and agents from `codex-template/skills/continuous-learning-manual/` (includes `agents/openai.yaml`).
- [ ] Write `codex-template/skills/ai-learning/config.json` with `observer.enabled: false`.
- [ ] Copy `skill.meta.json` from `codex-template/skills/continuous-learning-manual/` (no hooks entry).
- [ ] Write `codex-template/skills/ai-learning/SKILL.md` — Codex-framed: manual-first, observer optional.

### Workstream: remove old skill directories

- Status: `not-started`
- [ ] Delete `skills/continuous-learning-automatic/`.
- [ ] Delete `skills/continuous-learning-manual/`.
- [ ] Delete `codex-template/skills/continuous-learning-manual/`.

### Workstream: update references

- Status: `not-started`
- [ ] Update `.claude-plugin/plugin.json` — replace both old skill names with `ai-learning`.
- [ ] Update all package manifests (`packages/`) that reference `continuous-learning-automatic` or `continuous-learning-manual`.
- [ ] Update `scripts/install-mdt.js` — replace skill name constants and baseline/package skill lists.
- [ ] Update `scripts/mdt.js` — update any hardcoded skill path references that point to old skill directories.
- [ ] Update `AGENTS.md` if it references either old skill name.
- [ ] Grep repo-wide for remaining `continuous-learning-automatic` and `continuous-learning-manual` references; fix or note each one.

### Workstream: update tests

- Status: `not-started`
- [ ] Update `tests/scripts/install-mdt.test.js` — replace old skill names with `ai-learning`.
- [ ] Update `tests/scripts/install-mdt-unit.test.js` — same.
- [ ] Update `tests/scripts/continuous-learning-wrapper.test.js` — update skill root paths to `ai-learning`.
- [ ] Update `tests/scripts/mdt-cli.test.js` — update any skill path references.
- [ ] Update `tests/hooks/hooks.test.js` if it references old skill names.
- [ ] Run `npm test` — all 1297 tests must pass.

### Workstream: update docs

- Status: `not-started`
- [ ] Update `BACKLOG.md` — mark old skill split as resolved, note new unified skill.
- [ ] Update `docs/plans/active.md` — add this plan as `in-progress`.
- [ ] Check `docs/` for any references to old skill names and update.

## Verification

- [ ] `npm test` passes (1297 tests, 0 failures).
- [ ] `npm run lint` passes.
- [ ] `ls skills/ai-learning/` contains: SKILL.md, config.json, skill.meta.json, hooks/, scripts/, agents/.
- [ ] `ls codex-template/skills/ai-learning/` contains: SKILL.md, config.json, skill.meta.json, scripts/, agents/ (no hooks/).
- [ ] `codex-template/skills/ai-learning/agents/openai.yaml` exists.
- [ ] `skills/continuous-learning-automatic/` does not exist.
- [ ] `skills/continuous-learning-manual/` does not exist.
- [ ] `codex-template/skills/continuous-learning-manual/` does not exist.
- [ ] `grep -r "continuous-learning-automatic\|continuous-learning-manual" skills/ codex-template/ scripts/ tests/` returns no matches.
- [ ] Observer is enabled in `skills/ai-learning/config.json` (`"enabled": true`).
- [ ] Observer is disabled in `codex-template/skills/ai-learning/config.json` (`"enabled": false`).
- [ ] `hooks/observe.js` exists in `skills/ai-learning/` but not in `codex-template/skills/ai-learning/`.

## Follow-ups

- [ ] Rename commands that reference "continuous-learning" in their body text (learn.md, learn-eval.md) to reference `ai-learning` if needed.
- [ ] Consider whether `commands/learn.md` and `commands/learn-eval.md` should be moved inside `ai-learning/` as skill-owned commands or stay as top-level shared commands.
- [ ] Evaluate whether `SKILL.md` for ai-learning should be split into a short user-facing card + a detailed reference, to reduce cognitive load for first-time readers.
