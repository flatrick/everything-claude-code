# Skill Stocktake Consolidation

- Status: `not-started`
- Created: `2026-03-14`
- Last updated: `2026-03-14`
- Owner: `mdt`
- Execution mode: `manual`
- Related docs: `~/.claude/skills/`, `~/.claude/commands/`

## Goal

Clean up the global skill and command library based on the full stocktake run on 2026-03-14.
Four merges eliminate near-duplicate files. Three improvement tasks reduce overlap with
durable rules and clarify scope boundaries.

## Merges

### M1 — `documentation-steward` → `documentation-steward`

- **File to delete:** `~/.claude/skills/documentation-steward/SKILL.md` (and directory)
- **Why:** 95%+ identical to `documentation-steward`. documentation-steward is a strict superset — it adds
  the "Tool Docs Rule" section (L55–68) which documentation-steward lacks.
- **Side effect:** `docs-health.md` command references `documentation-steward` — fixed by M2.
- **Steps:**
  - [ ] Verify no unique content in documentation-steward not present in documentation-steward
  - [ ] Delete `~/.claude/skills/documentation-steward/` directory

### M2 — `docs-health.md` → `docs-audit.md`

- **File to delete:** `~/.claude/commands/docs-health.md`
- **Why:** Near-identical command (80 vs 77 lines). docs-health.md references
  `documentation-steward` (retired by M1). Its one unique addition — a reference to the
  `tool-doc-maintainer` skill — should move to docs-audit.md.
- **Steps:**
  - [ ] Add `tool-doc-maintainer` skill reference to step 3 of `docs-audit.md`
  - [ ] Delete `~/.claude/commands/docs-health.md`

### M3 — `learn.md` → `ai-learning.md`

- **File to delete:** `~/.claude/commands/learn.md`
- **Why:** 21-line thin wrapper that delegates entirely to the ai-learning workflow.
  `ai-learning.md` already covers the same entrypoint with CLI commands and more detail.
- **Unique content to preserve:** learn.md mentions `/learn-eval` as a quality-gated
  follow-up — add that note to ai-learning.md.
- **Steps:**
  - [ ] Add `/learn-eval` follow-up note to `~/.claude/commands/ai-learning.md`
  - [ ] Delete `~/.claude/commands/learn.md`

### M4 — `mdt-dev-smoke.md` → `smoke.md`

- **File to delete:** `~/.claude/commands/mdt-dev-smoke.md`
- **Why:** 95%+ identical to smoke.md (98 vs 94 lines). Only unique addition is the
  cross-tool audit constraint: "do not probe Claude, Cursor, and Codex from the same run
  unless the user explicitly asks for the all-tools audit."
- **Steps:**
  - [ ] Add cross-tool audit constraint to `~/.claude/commands/smoke.md` Required Behavior section
  - [ ] Delete `~/.claude/commands/mdt-dev-smoke.md`

## Improvements

### I1 — `api-design` + `backend-patterns` scope dedup

- **Files:** `~/.claude/skills/api-design/SKILL.md` (523 lines),
  `~/.claude/skills/backend-patterns/SKILL.md` (598 lines)
- **Problem:** backend-patterns claims "API design" in its description, creating scope
  ambiguity. REST naming, status codes, and versioning likely appear in both.
- **Steps:**
  - [ ] Read both files in full and identify duplicated REST/API sections
  - [ ] Remove the duplicate content from backend-patterns, leaving api-design as the
        REST reference
  - [ ] Update backend-patterns description to remove "API design"

### I2 — `coding-standards` overlap with durable rules

- **File:** `~/.claude/skills/coding-standards/SKILL.md` (530 lines)
- **Problem:** Intro/general sections duplicate `common/coding-style.md` durable rules
  (immutability, error handling, input validation, file organization). These rules are
  always loaded and do not need repeating in the skill.
- **Steps:**
  - [ ] Identify sections that mirror coding-style.md
  - [ ] Remove ~100 lines of overlapping intro content
  - [ ] Keep TypeScript/JS/React-specific implementation patterns only

### I3 — `project-guidelines-example` purpose clarification

- **File:** `~/.claude/skills/project-guidelines-example/SKILL.md` (350 lines)
- **Problem:** No clear trigger condition. Users may invoke it expecting live workflow
  guidance when it is actually a template reference for skill-create.
- **Steps:**
  - [ ] Add a `## Purpose` section at the top clarifying: "This is a template reference
        for use with skill-create, not a live workflow skill. Do not invoke directly."
  - [ ] Optionally: consider moving to `docs/` if it is never meant to be invoked

## Inventory snapshot (2026-03-14)

| Path | Count |
|------|-------|
| `~/.claude/skills/` | 24 skills |
| `~/.claude/commands/` | 19 commands |

After merges: 23 skills, 16 commands.

## Cursor results and conclusions

**Scope:** Full stocktake run against Cursor config only: `~/.cursor/skills/` (26 files). Project-level `{cwd}/.cursor/skills/` was not present for the MDT repo, so global skills only.

**Verdicts:** 25 **Keep**, 1 **Update**. No Retire or Merge recommended.

### Update (Cursor run)

- **deployment-patterns** — GitHub Actions examples use `actions/checkout@v4` and `actions/setup-node@v4`. Newer majors (e.g. checkout v5/v6, setup-node v5) exist with better credential handling and Node 24. **Action:** Update CI snippets in the skill to v5 or add a short note and link to actions changelogs; Dockerfile Node 22 is current LTS and fine.

### Relation to Claude’s plan

- **Merges (M1–M4):** All target `~/.claude/skills/` and `~/.claude/commands/`. Those paths were not in Cursor’s scan, so the same merge targets do not exist in Cursor’s inventory (e.g. no `documentation-steward`, no `docs-health.md`, `learn.md`, or `mdt-dev-smoke.md` under `~/.cursor/`). If skills/commands are later synced from a single source, performing M1–M4 in that source would keep Cursor in sync.
- **Improvements (I1–I3):** api-design, backend-patterns, coding-standards, and project-guidelines-example are present in Cursor’s global skills. Claude’s improvement tasks (scope dedup, overlap with durable rules, purpose clarification) apply to the same skill names and could be carried over to the shared/MDT skill source so both tools benefit.

### Conclusions

1. **Apply the deployment-patterns update** in the skill source (or in `~/.cursor/skills/deployment-patterns/` if Cursor-only): refresh or note GitHub Actions versions as above.
2. **Optional:** If skills are maintained in one place for both Claude and Cursor, implement Claude’s I1–I3 (api-design/backend-patterns dedup, coding-standards trim, project-guidelines-example purpose) there so Cursor’s copies stay aligned.
3. **Results cache:** Cursor run wrote `~/.cursor/skills/skill-stocktake/results.json`. Future Cursor stocktakes will do Quick Scan unless that file is removed or a full run is requested.
