---
description: Promote project instincts to global scope
agent: build
---

# Promote Command

Promote instincts in continuous-learning-manual: $ARGUMENTS

## Your Task

Run:

```bash
node "${MDT_ROOT}/skills/continuous-learning-manual/scripts/instinct-cli.js" promote $ARGUMENTS
```

For manual installs, replace `<config>` with your MDT config directory:

```bash
node "<config>/skills/continuous-learning-manual/scripts/instinct-cli.js" promote $ARGUMENTS
```

