interface:
  display_name: "Smoke"
  short_description: "Run the installed MDT Codex smoke checks"
  brand_color: "#10B981"
  default_prompt: "Run the installed Codex MDT smoke checks. Use `node ~/.codex/mdt/scripts/mdt.js smoke tool-setups --tool codex` and `node ~/.codex/mdt/scripts/mdt.js smoke workflows --tool codex`, then summarize PASS/FAIL/SKIP with the next remediation step."
policy:
  allow_implicit_invocation: true
